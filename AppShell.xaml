<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Vehicle_Maintenance_App.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Vehicle_Maintenance_App"
    Title="Vehicle Maintenance App">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:Views.MainPage}"
        Route="MainPage" />
    <ShellContent 
        Title="Add New Vehicle"
        ContentTemplate="{DataTemplate local:Views.AddVehiclePage}"
        Route="AddVehiclePage" />

</Shell>
